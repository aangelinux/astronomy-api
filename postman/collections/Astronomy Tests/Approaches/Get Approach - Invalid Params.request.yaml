$kind: graphql-request
name: Get Approach - Invalid Params
url: "{{URL}}"
query: |
  query Close_approach {
      close_approach(id: null) {
          spkid
          date
          nominal_distance_km
          minimum_distance_km
          relative_velocity_km_s
          rarity
      }
  }
variables: ""
schema:
  source: auto
scripts:
  - type: afterResponse
    code: "eval(pm.collectionVariables.get(\"status\"))\r

      eval(pm.collectionVariables.get(\"errors\"))\r

      eval(pm.collectionVariables.get(\"returnsErrorMessage\"))\r

      \r

      hasStatus(400)\r

      errors()\r

      returnsErrorMessage(\"Expected value of type \\\"ID!\\\", found null.\")"
    language: text/javascript
order: 634263100761665
