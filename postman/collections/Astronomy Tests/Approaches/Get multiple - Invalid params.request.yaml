$kind: graphql-request
name: Get multiple - Invalid params
url: "{{URL}}"
query: |
  query Close_approaches {
      close_approaches(page: null) {
          id
          spkid
          date
          nominal_distance_km
          minimum_distance_km
          relative_velocity_km_s
          rarity
      }
  }
variables: ""
schema:
  source: auto
scripts:
  - type: afterResponse
    code: "eval(pm.collectionVariables.get(\"status\"))\r

      eval(pm.collectionVariables.get(\"errors\"))\r

      eval(pm.collectionVariables.get(\"returnsErrorMessage\"))\r

      \r

      hasStatus(400)\r

      errors()\r

      returnsErrorMessage(\"Expected value of type \\\"Int!\\\", found null.\")"
    language: text/javascript
order: 2869438039212033
