$kind: graphql-request
name: Register - Duplicate user
url: "{{URL}}"
query: |
  mutation Register {
      register(input: { username: "init", password: {{password}} }) {
          userid
          username
          created
      }
  }
variables: ""
schema:
  source: auto
scripts:
  - type: afterResponse
    code: "pm.test(\"Returns error 'Duplicate entry'\", () => {\r

      \    pm.expect(pm.response.errors).to.not.be.empty;\r

      \    pm.expect(pm.response.errors[0].message).to.contain(\"Duplicate
      entry\")\r

      })"
    language: text/javascript
order: 4497938831980465
