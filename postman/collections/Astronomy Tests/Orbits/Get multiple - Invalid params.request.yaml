$kind: graphql-request
name: Get multiple - Invalid params
url: "{{URL}}"
query: |
  query Orbits {
      orbits(page: {{page}}) {
          spkid
          orbital_class
          eccentricity
          years
      }
  }
variables: ""
schema:
  source: auto
scripts:
  - type: afterResponse
    code: "const jsonResponse = pm.response.data\r

      const orbits = jsonResponse.orbits\r

      \r

      eval(pm.collectionVariables.get(\"status\"))\r

      eval(pm.collectionVariables.get(\"success\"))\r

      eval(pm.collectionVariables.get(\"returnsMultipleObjects\"))\r

      eval(pm.collectionVariables.get(\"isAstronomicalObject\"))\r

      \r

      hasStatus(200)\r

      success()\r

      returnsMultipleObjects(orbits)\r

      isAstronomicalObject(orbits)"
    language: text/javascript
order: 4261432302439390
